; SMTP Forward Configuration
; This configures how Haraka delivers outbound mail

; Deliver directly to destination MX servers
enable_outbound=true

; Timeout for connections
timeout=300

; Max connections per domain
max_connections=10

; Connection pool
pool_timeout=300

; Number of concurrent deliveries
concurrency_max=10
